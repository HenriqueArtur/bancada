{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the webview is allowed to reach beyond this app's own commands. Short on purpose: the seam is the folder full of commands, and a plugin permission is a second seam nobody reviews.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
